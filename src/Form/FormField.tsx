// import type { ComponentProps } from 'react';

// import { FormFieldContext } from '@/lib/components/ui/form';
// import { useFormStore } from '@/Form/useFormStore';

// export interface FormFieldProps
//   extends Omit<ComponentProps<typeof FormFieldContext>, 'control'> {}

// export const FormField = ({ name, ...formFieldProps }: FormFieldProps) => {
//   const control = useFormStore((state) => state.control);
//   return <SFormField name={name} control={control} {...formFieldProps} />;
// };
